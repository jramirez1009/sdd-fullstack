## 1. Capa de servicios de la API

- [x] 1.1 Añadir a `frontend/src/servicios/api.js` `listarTareas(filtros)` (`GET /api/tareas`) que construye la query con `URLSearchParams` omitiendo las claves de valor neutro y serializando la lista `etiquetas` en el formato que espera el backend. Verificar: leyendo el parser del backend, la query generada para un conjunto de filtros de ejemplo coincide con lo que el endpoint acepta; `npm run build` en `frontend/` compila.
- [x] 1.2 Añadir `crearTarea(datos)` (`POST /api/tareas`), `editarTarea(id, datos)` (`PUT /api/tareas/:id`), `cambiarCompletada(id, done)` (`PATCH /api/tareas/:id/completar`, cuerpo `{ completada: done }`) y `eliminarTarea(id)` (`DELETE /api/tareas/:id`), todas sobre `peticion(...)` sin marca `esAutenticacion`. Verificar: cada función usa ruta y método correctos y `eliminarTarea` tolera el `204` sin cuerpo (devuelve `null`).
- [x] 1.3 Añadir `listarEtiquetas()` (`GET /api/etiquetas`) y `crearEtiqueta(nombre)` (`POST /api/etiquetas`, cuerpo `{ nombre }`). Verificar: `listarEtiquetas` devuelve el array directo del backend y `crearEtiqueta` propaga el `ErrorApi` con su `codigo` cuando el nombre ya existe (`409`).

## 2. Mover el diálogo de confirmación a Comunes/

- [x] 2.1 `git mv frontend/src/componentes/Categoria/DialogoConfirmarBorrado.jsx` y `DialogoConfirmarBorrado.module.css` a `frontend/src/componentes/Comunes/`. Parametrizar su texto por props (`titulo`, `descripcion`, `etiquetaConfirmar`, `etiquetaEnCurso`) y cambiar `alConfirmar` a una función sin argumentos, conservando intactos el foco atrapado, el cierre con `Esc`, `role="alertdialog"`, la guarda contra doble envío y el `<Cargando>`/`<MensajeError>` durante la promesa. Verificar: el componente ya no contiene ningún texto fijo referido a "categoría".
- [x] 2.2 Actualizar `frontend/src/componentes/Categoria/ListaCategorias.jsx`: importar el diálogo desde `../Comunes/`, pasar los textos actuales por props (título "Eliminar categoría" y la explicación de que las tareas quedan sin categoría) y `alConfirmar={() => eliminar(categoria.id)}`. Verificar: eliminar una categoría con confirmación funciona exactamente igual que antes (diálogo, texto sobre las tareas, cierre tras éxito, error con reintento); `npm run build` compila.

## 3. Hook `useEtiquetas`

- [x] 3.1 Crear `frontend/src/hooks/useEtiquetas.js` que al montar hace `listarEtiquetas()` una vez y mantiene `etiquetas` (array, `[]` por defecto), `cargando`, `error` y `recargar`, y expone `crear(nombre)` que hace `crearEtiqueta(nombre)`, añade la etiqueta devuelta a `etiquetas` en orden por nombre y propaga su error con `throw`. Verificar: al montar con la API disponible `etiquetas` queda poblado; tras un `crear` exitoso la nueva etiqueta aparece en `etiquetas` sin recargar; un `crear` que rechaza deja `etiquetas` intacto y la promesa rechaza con el `ErrorApi`.

## 4. Hook `useTareas`

- [x] 4.1 Crear `frontend/src/hooks/useTareas.js` que recibe `filtros` (objeto ya normalizado a nombres de query), al montar y cada vez que cambia la versión serializada de `filtros` hace `listarTareas(filtros)`, y mantiene `tareas` (array, `[]`), `cargando`, `error` (el `ErrorApi` de la carga, incluido el `400` por parámetro inválido) y `recargar`. Verificar: cambiar un valor de `filtros` dispara una nueva consulta con los parámetros correspondientes; pasar el mismo `filtros` con otra identidad de objeto no dispara consulta.
- [x] 4.2 Añadir cancelación de peticiones fuera de orden (contador de secuencia o `AbortController`) para que una respuesta lenta de una consulta anterior no pise a una más reciente. Verificar: con dos cambios de filtro seguidos y la primera respuesta llegando después de la segunda, la lista muestra el resultado del segundo filtro.
- [x] 4.3 Añadir al hook `crear(datos)`, `editar(id, datos)`, `eliminar(id)` y `cambiarCompletada(id, completada)` que llaman a la capa de servicios y, **solo tras éxito**, reconcilian la lista con `recargar`; las acciones propagan su error con `throw` en lugar de guardarlo en `error`. Verificar: tras un `crear` exitoso la lista se recarga con los filtros vigentes; un `cambiarCompletada` exitoso deja la tarea con el nuevo estado; una acción que rechaza deja la lista intacta y la promesa rechaza con el `ErrorApi`.

## 5. Autocompletar de etiquetas

- [x] 5.1 Crear `frontend/src/componentes/Tarea/AutocompletarEtiquetas.jsx` (+ `.module.css`) que recibe la selección actual (array de objetos etiqueta) y `alCambiar`, usa `useEtiquetas()` para sugerir por coincidencia de subcadena insensible a caja excluyendo las ya seleccionadas, permite elegir una sugerencia con ratón o teclado (flechas + Enter) y quitar una etiqueta seleccionada (botón o Backspace con input vacío). Verificar: escribir parte del nombre de una etiqueta existente la muestra como sugerencia y elegirla la añade a la selección; quitar una etiqueta la retira.
- [x] 5.2 Cuando el texto recortado no coincide exactamente con ninguna etiqueta y no está vacío, ofrecer "Crear «texto»"; al elegirla, `await crear(texto)` de `useEtiquetas`, y en éxito añadir la etiqueta devuelta a la selección y limpiar el input; en error mostrar el motivo junto al campo sin añadir nada. Verificar: crear una etiqueta nueva al vuelo la deja seleccionada sin salir del formulario; un nombre duplicado (`409`) muestra el aviso junto al campo y no la añade.

## 6. Formulario de crear / editar tarea

- [x] 6.1 Crear `frontend/src/componentes/Tarea/FormularioTarea.jsx` (+ `.module.css`) con campos título (obligatorio), descripción, prioridad, fecha de vencimiento y etiquetas (vía `AutocompletarEtiquetas`); opera en modo crear si no recibe `tarea` y en modo editar si la recibe, precargando todos los valores actuales. No ofrece cambiar `completada`. Verificar: en modo editar los campos aparecen precargados; enviar con título vacío o solo espacios marca el campo y no dispara ninguna petición.
- [x] 6.2 Añadir el selector de categoría alimentado por `useCategorias()` (las categorías del usuario más una opción "Sin categoría"); no se reimplementa la llamada a `/api/categorias`. Verificar: el selector lista las categorías del usuario; guardar con "Sin categoría" envía la tarea sin categoría y la lista la muestra sin categoría.
- [x] 6.3 En el envío, envolver la llamada al hook (`crear`/`editar`) en `try/catch`: repartir un `400` con `detalles` por campo (`titulo`, `prioridad`, `fecha_vencimiento`) junto al control correspondiente conservando lo escrito; el código de referencia inválida (categoría o etiqueta ajena) y cualquier otro fallo se muestran con `<MensajeError>` general del formulario. Verificar: forzar un `400` por prioridad inválida muestra el mensaje junto al selector de prioridad; un fallo de red muestra el `<MensajeError>` general y el formulario no se cierra.
- [x] 6.4 Mantener un estado local `enviando` que deshabilita el botón de envío y muestra `<Cargando>` mientras la promesa está pendiente, impidiendo el doble envío; al tener éxito, notificar al contenedor para que cierre el formulario. Verificar: pulsar "Guardar" dos veces solo genera una petición; tras el éxito el formulario se cierra y la tarea aparece/cambia en la lista.

## 7. Fila de tarea

- [x] 7.1 Crear `frontend/src/componentes/Tarea/ItemTarea.jsx` (+ `.module.css`) que muestra título, categoría (chip solo si `categoria` no está vacía), prioridad, fecha de vencimiento (solo si la hay) y las etiquetas (chips), tratando `id` siempre como cadena. Ofrece acciones de editar y eliminar que invocan los callbacks recibidos. Verificar: una tarea sin categoría ni etiquetas se muestra sin huecos ni adornos de esos campos; una tarea con ambos los muestra.
- [x] 7.2 Añadir el control de completar (checkbox o botón) que llama a `alCambiarCompletada(tarea.id, !tarea.completada)` calculando el booleano una sola vez sobre el dato renderizado, con una guarda (`enVuelo`/`procesando`) que impide la doble activación mientras la promesa está pendiente; si falla, mostrar el motivo con `<MensajeError>` y conservar el estado anterior. Verificar: marcar y desmarcar una tarea envía el booleano explícito a `PATCH …/completar` y refleja el nuevo estado sin recargar; doble clic rápido genera una sola petición.

## 8. Panel de filtros

- [x] 8.1 Crear `frontend/src/componentes/Tarea/FiltroTareas.jsx` (+ `.module.css`) con los controles: estado (todas/completadas/pendientes), categoría (de `useCategorias` + "Sin categoría" → `ninguna`), prioridad, `fecha_vencimiento_desde`, `fecha_vencimiento_hasta`, etiquetas (multi-selección sobre `useEtiquetas`), ordenar y dirección. Cada cambio de un control llama a `alCambiarFiltros` en el acto, sin botón de aplicar. Verificar: cambiar cualquier control provoca de inmediato una nueva consulta `GET /api/tareas` con el conjunto completo de filtros activos; volver un control a su valor neutro quita ese parámetro de la consulta.
- [x] 8.2 Añadir el campo de búsqueda por texto con debounce: el input responde en cada tecla, pero el valor solo entra en los filtros efectivos tras 300–400 ms sin escribir; un valor vacío se omite. Verificar: escribir "compra" carácter a carácter genera una sola petición tras la pausa, no una por tecla; borrar el texto dispara una consulta sin el parámetro de búsqueda.
- [x] 8.3 Mostrar el selector de categoría del filtro deshabilitado mientras `useCategorias` carga y no bloquear el resto de filtros si su carga falla. Verificar: con la carga de categorías fallando, los demás filtros y la lista de tareas siguen usables.

## 9. Pantalla de lista y orquestación

- [x] 9.1 Crear `frontend/src/componentes/Tarea/ListaTareas.jsx` (+ `.module.css`) que posee el estado de interfaz de los filtros, deriva de él el objeto `filtros` normalizado, instancia `useTareas(filtros)`, renderiza `FiltroTareas` siempre visible y el cuerpo según sub-estado: `cargando` → `<Cargando texto="Cargando tareas…" />`; `error` de carga → `<MensajeError>` + "Reintentar" → `recargar`; vacío sin filtros → "Aún no tienes tareas" + crear la primera; vacío con filtros → "No hay tareas que cumplan los filtros"; con datos → un `<ItemTarea>` por tarea. Verificar: cada sub-estado se observa forzando su condición (API lenta, API caída, `400` por parámetro, usuario sin tareas, filtros sin resultados, usuario con tareas).
- [x] 9.2 Orquestar el formulario y el diálogo desde `ListaTareas`: estado `formulario` (`null` | `{ modo: 'crear' }` | `{ modo: 'editar', tarea }`) y `aEliminar` (`null` | `tarea`); el diálogo usa `DialogoConfirmarBorrado` de `Comunes/` con textos de tarea y `alConfirmar={() => eliminar(tarea.id)}`; tras cada acción exitosa la lista ya está reconciliada por el hook. Verificar: crear, editar, eliminar (con confirmación) y completar desde la lista reflejan el resultado de inmediato respetando los filtros y el orden vigentes, sin recargar la página; cancelar el diálogo no llama a la API.

## 10. Integración de ruta y limpieza del placeholder

- [x] 10.1 En `frontend/src/App.jsx` cambiar la ruta hija `/tareas` para renderizar `<ListaTareas />` y quitar el import de `paginas/Tareas.jsx`, sin tocar `RutaProtegida`, el `Sidebar` (el enlace a `/tareas` ya existe) ni el comodín `*`. Verificar: abrir `/tareas` con sesión muestra la pantalla dentro del armazón con el enlace "Tareas" activo; sin sesión redirige a `/login`.
- [x] 10.2 Eliminar `frontend/src/paginas/Tareas.jsx` y `frontend/src/paginas/Tareas.module.css`, y retirar la carpeta `frontend/src/paginas/` si queda vacía. Verificar: no quedan imports a `paginas/` en el árbol; `npm run build` compila sin errores.

## 11. Verificación integral

- [x] 11.1 Recorrido manual completo: entrar a Tareas desde el Sidebar con un usuario sin tareas → estado vacío → crear una tarea solo con título → crear otra con categoría, descripción, fecha, prioridad y una etiqueta creada al vuelo → editar una → completar y descompletar → filtrar por estado, categoría (incluida "Sin categoría"), prioridad, rango de fecha y etiquetas comprobando que cada cambio dispara la consulta al instante → buscar un texto y confirmar una sola petición tras la pausa → combinar búsqueda + filtros + orden + dirección → eliminar una confirmando y ver que desaparece al instante → forzar un fallo de red en la carga inicial y usar "Reintentar" → recargar en `/tareas` con sesión → comprobar que la confirmación de borrado de categorías sigue igual tras mover el diálogo. Verificar: todos los escenarios de `openspec/changes/add-frontend-tareas/specs/frontend-tareas/spec.md` se observan.
- [x] 11.2 Ejecutar `npm run build` en `frontend/` y `openspec validate add-frontend-tareas --strict`. Verificar: build sin errores y validación sin fallos.
