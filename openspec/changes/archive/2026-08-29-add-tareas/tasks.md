## 1. Esquema: búsqueda de texto completo

- [x] 1.1 Comprobar contra la base de Supabase que la configuración de texto `spanish` está disponible, ejecutando `SELECT to_tsvector('spanish', 'corriendo por el parque')` y verificando que devuelve lexemas lematizados; si no lo estuviera, registrar la limitación en `design.md` y continuar con `simple`
- [x] 1.2 Añadir a `backend/bd/schema.sql` la columna generada `busqueda_tsv tsvector GENERATED ALWAYS AS (to_tsvector('spanish', coalesce(titulo,'') || ' ' || coalesce(descripcion,''))) STORED` sobre `tareas`, con `ADD COLUMN IF NOT EXISTS`, y verificar que el archivo sigue siendo idempotente ejecutándolo dos veces seguidas sin error
- [x] 1.3 Añadir el índice `CREATE INDEX IF NOT EXISTS tareas_usuario_busqueda_idx ON tareas USING GIN (busqueda_tsv)` y actualizar el bloque de reversión comentado del final del archivo con el `DROP INDEX` y el `DROP COLUMN` correspondientes
- [x] 1.4 Aplicar el esquema con `node bd/ejecutar-schema.js` y verificar que la columna se ha rellenado sola para las filas de tareas ya existentes, consultando `SELECT id, busqueda_tsv FROM tareas LIMIT 5`
- [x] 1.5 Verificar con datos de prueba desechables que modificar el título o la descripción de una tarea actualiza `busqueda_tsv` sin ninguna acción adicional, y que un `EXPLAIN` de la búsqueda por `busqueda_tsv @@ websearch_to_tsquery(...)` usa el índice GIN y no un recorrido secuencial; eliminar los datos al terminar

## 2. Errores, transacciones y validación compartidos

- [x] 2.1 Añadir `REFERENCIA_INVALIDA` al catálogo `CODIGOS_ERROR` de `backend/src/utils/errores.js` y un constructor `errorReferenciaInvalida(recurso)` que devuelva un `ErrorAplicacion` de estado `400`; verificar que el mensaje no menciona ninguna tabla ni restricción y que no distingue "no existe" de "es de otro usuario"
- [x] 2.2 Añadir a `backend/src/config/bd.js` un ayudante `conTransaccion(fn)` que tome un cliente del pool, emita `BEGIN`, invoque `fn(cliente)` y haga `COMMIT` o `ROLLBACK` según el resultado, liberando el cliente siempre; verificar que una función que lanza deja la base sin cambios y que el cliente se libera en ambos caminos (comprobar que `pool.idleCount` vuelve a su valor previo)
- [x] 2.3 Añadir a `backend/src/utils/validacion.js` una función `validarTarea(cuerpo)` que exija título presente, de tipo texto, no vacío tras recortar y de como mucho 200 caracteres contados en puntos de código; que acepte descripción opcional de hasta 2000; prioridad opcional entre `baja`, `media` y `alta` con `media` por defecto; y `fecha_vencimiento` opcional con forma `AAAA-MM-DD` válida; devolviendo el mismo `{ valido, detalles, datos }` que las validaciones existentes
- [x] 2.4 Hacer que `validarTarea` acepte `categoria_id` opcional (identificador o vacío) y `etiquetas` opcional (array de identificadores), distinguiendo el campo ausente del array vacío mediante la presencia de la clave en el cuerpo; verificar que un `categoria_id` mal formado, un `etiquetas` que no es array y un elemento de `etiquetas` que no es identificador se rechazan con `400`
- [x] 2.5 Verificar que `validarTarea` ignora en silencio `usuario_id`, `estado`, `completada`, `creado_en`, `actualizado_en` y `completada_en` si llegan en el cuerpo, y que ninguno de ellos aparece en los datos normalizados que devuelve
- [x] 2.6 Añadir `validarEstadoCompletada(cuerpo)` para el cuerpo de `PATCH .../completar`, que exija el campo `completada` presente y estrictamente booleano; verificar que el campo ausente, `null`, `"true"` y `1` se rechazan con `400`
- [x] 2.7 Añadir `validarConsultaTareas(query)` que valide y normalice `completada`, `categoria`, `prioridad`, `fecha_vencimiento_desde`, `fecha_vencimiento_hasta`, `busqueda`, `etiquetas`, `ordenar` y `direccion`, rechazando con `400` y señalando el parámetro cualquier valor fuera de su conjunto admitido, e ignorando los parámetros desconocidos; verificar caso por caso que `ordenar=password`, `direccion=aleatoria`, `prioridad=urgente`, `completada=quizas`, `fecha_vencimiento_desde=ayer` y `categoria=abc` producen `400` y no un resultado
- [x] 2.8 Hacer que `validarTarea` recorte los extremos del título y de la descripción, los normalice a Unicode NFC, cuente su longitud en puntos de código (`[...texto].length`), rechace el título que contenga saltos de línea o caracteres de control, y rechace la descripción que contenga caracteres de control distintos del salto de línea; verificar que un título de 200 emojis se acepta —donde `.length` de JavaScript daría 400 y lo rechazaría—, que uno de 201 caracteres se rechaza, que `"a\nb"` como título se rechaza y como descripción se acepta, y que un texto en NFD sale idéntico a su equivalente en NFC
- [x] 2.9 Hacer que `validarConsultaTareas` trate la `busqueda` vacía o de solo espacios como parámetro ausente en lugar de rechazarla; que rechace un filtro de valor único que llegue repetido (Express lo entrega como array); que acote `busqueda` a 200 caracteres y las listas de etiquetas —la del filtro y la del cuerpo— a 50 elementos; y que aplique la dirección de ordenación por defecto de cada campo (`desc` para `creado_en` y `prioridad`, `asc` para `titulo` y `fecha_vencimiento`) solo cuando `direccion` no viene. Verificar cada caso por separado, incluida la precedencia de la dirección explícita

## 3. Repositorio de tareas: lectura

- [x] 3.1 Crear `backend/src/repositorios/tareasRepo.js` con la lista explícita de columnas públicas de una tarea y la consulta base del listado: `SELECT` con `LEFT JOIN categorias` para la categoría embebida y una subconsulta lateral con `json_agg` que devuelva las etiquetas como array de objetos con `id`, `nombre` y `creado_en`, y `[]` cuando no hay ninguna; verificar por inspección que no usa `SELECT *` ni `to_jsonb(e.*)`, de modo que `usuario_id` no puede filtrarse a la respuesta
- [x] 3.2 Hacer que la consulta añada siempre y en primer lugar `WHERE t.usuario_id = $1`, antes de mirar ningún filtro; verificar por inspección que no existe ningún camino de código que construya el listado sin esa condición
- [x] 3.3 Añadir la construcción de los filtros como fragmentos literales que empujan su valor al array de parámetros: `completada` (traducido a `estado = 'completada'` o distinto), `categoria`, `prioridad`, `fecha_vencimiento_desde` y `fecha_vencimiento_hasta` (ambos inclusivos); verificar por inspección que ningún valor del cliente aparece en el texto SQL y que el número de marcador sale de la longitud del array de parámetros
- [x] 3.4 Añadir el filtro `busqueda` como `t.busqueda_tsv @@ websearch_to_tsquery('spanish', $N)`; verificar contra la base con datos desechables que encuentra por título y por descripción, ignorando caja y acentos, que reconoce una variante flexiva de la palabra, y que una cadena con comillas y signos sueltos devuelve resultado y no error
- [x] 3.5 Añadir el filtro `etiquetas` por nombre y con semántica de conjunción, como una condición que exige que el número de etiquetas del usuario con esos nombres asociadas a la tarea iguale el número de nombres pedidos, pasando los nombres con `= ANY($N)`; verificar por inspección que no se generan marcadores `IN (...)` a partir de la longitud de la lista del cliente, y contra la base que pedir dos nombres devuelve solo las tareas que tienen ambos
- [x] 3.6 Añadir la ordenación resolviendo `ordenar` y `direccion` contra dos mapas constantes del módulo, con `creado_en` descendente por defecto, el `CASE` de orden de negocio para prioridad (alta, media, baja), `NULLS LAST` en fecha de vencimiento en ambas direcciones, y `, t.id DESC` como desempate final en todos los casos; verificar por inspección que ningún valor recibido se interpola en el `ORDER BY` y que una clave ausente del mapa no produce SQL alternativo
- [x] 3.7 Hacer que el filtro `categoria` acepte el valor reservado `ninguna` produciendo la condición `t.categoria_id IS NULL` en lugar de una igualdad parametrizada, y que ese valor no se pase nunca como parámetro a la consulta; verificar con datos desechables que devuelve exactamente las tareas sin categoría y ninguna otra
- [x] 3.8 Añadir `obtenerTarea({ id, usuarioId })` que reutilice la misma proyección del listado para devolver una tarea concreta ya con su categoría y sus etiquetas, o `null` si no hay tal fila suya; verificar con datos desechables que el identificador de una tarea de otro usuario devuelve `null`

## 4. Repositorio de tareas: escritura

- [x] 4.1 Añadir `verificarCategoriaDelUsuario(cliente, { categoriaId, usuarioId })` y `verificarEtiquetasDelUsuario(cliente, { etiquetaIds, usuarioId })`, la segunda con `SELECT id FROM etiquetas WHERE usuario_id = $1 AND id = ANY($2)` comparando cuántas volvieron con cuántas se pidieron; verificar con datos desechables que una etiqueta ajena y una inexistente producen el mismo resultado negativo
- [x] 4.2 Añadir `crearTarea({ usuarioId, datos })` que se ejecute dentro de `conTransaccion`: verificar las referencias, insertar la tarea con `RETURNING`, insertar las filas de `tarea_etiquetas` y devolver la tarea ya compuesta; verificar con datos desechables que la tarea nace con `estado = 'pendiente'`, `completada_en` vacío y la prioridad indicada o `media`
- [x] 4.3 Hacer que la inserción de `tarea_etiquetas` no produzca asociaciones duplicadas cuando el mismo identificador llega repetido en la lista; verificar con datos desechables que enviar dos veces la misma etiqueta deja una sola fila de vínculo
- [x] 4.4 Añadir `actualizarTarea({ id, usuarioId, datos })` dentro de `conTransaccion`, con `UPDATE ... WHERE id = $N AND usuario_id = $M`, devolviendo `null` cuando no hay fila suya, y sustituyendo el conjunto de etiquetas solo si el campo venía presente en la petición; verificar con datos desechables que omitir el campo conserva las etiquetas, que una lista vacía las quita todas, y que una lista nueva sustituye a la anterior sin borrar ninguna etiqueta en sí
- [x] 4.5 Verificar con datos desechables que una etiqueta ajena entre varias propias hace fallar la operación completa: la transacción se revierte y ni la tarea ni ninguna de las asociaciones queda escrita, ni en creación ni en edición
- [x] 4.6 Añadir `fijarCompletada({ id, usuarioId, completada })` como un único `UPDATE` que fije `estado` y `completada_en` a la vez con expresiones `CASE` sobre el booleano recibido, devolviendo `null` cuando no hay fila suya; verificar con datos desechables que completar registra el instante, que descompletar lo vacía y deja la tarea en `pendiente`, y que repetir la misma petición deja el mismo estado
- [x] 4.7 Añadir `eliminarTarea({ id, usuarioId })` como `DELETE ... WHERE id = $1 AND usuario_id = $2` devolviendo si se eliminó alguna fila; verificar con datos desechables que los vínculos de `tarea_etiquetas` desaparecen por la cascada, que la categoría y las etiquetas siguen existiendo, y que borrar el identificador de una tarea ajena devuelve falso dejando esa fila intacta
- [x] 4.8 Verificar que todas las funciones del archivo exigen `usuarioId` en su firma, que ninguna consulta carece de `usuario_id` en su `WHERE`, y que cada consulta es una cadena literal con marcadores de parámetro y no una cadena compuesta con datos de la petición

## 5. Endpoints de tareas

- [x] 5.1 Crear `backend/src/controladores/tareasControlador.js` con `listar`, `crear`, `actualizar`, `fijarCompletada` y `eliminar`, tomando siempre el identificador de usuario de `req.usuario.id` y delegando el formato de error al middleware con `siguiente(error)`; verificar por inspección que ningún controlador lee un identificador de usuario del cuerpo, de la query ni de la ruta
- [x] 5.2 Hacer que `actualizar`, `fijarCompletada` y `eliminar` lancen `errorNoEncontrado` cuando el repositorio no devuelve fila, sin ninguna rama que distinga "no existe" de "es de otro usuario"; verificar por inspección que existe un único camino de código hacia ese `404` en cada uno
- [x] 5.3 Crear `backend/src/rutas/tareasRutas.js` con `GET /`, `POST /`, `PUT /:id`, `PATCH /:id/completar` y `DELETE /:id`, y montarlo en `backend/src/app.js` como `app.use('/api/tareas', requiereAutenticacion, tareasRutas)`; verificar que los cinco endpoints responden `401` sin token y con un token manipulado
- [x] 5.4 Verificar contra el servidor en marcha que `POST /api/tareas` con solo el título responde `201` y devuelve la tarea con descripción, categoría y fecha vacías, prioridad `media`, `completada` en falso y `etiquetas` como array vacío
- [x] 5.5 Verificar que `POST /api/tareas` con todos los campos —categoría propia y etiquetas propias— responde `201` y devuelve la categoría como objeto y las etiquetas como array de objetos con `id`, `nombre` y `creado_en`
- [x] 5.6 Verificar que `POST /api/tareas` sin título, con título vacío, con título de 201 caracteres, con prioridad `urgente` y con una fecha de vencimiento mal formada responde `400` en los cinco casos señalando el campo, y que no crea ninguna tarea
- [x] 5.7 Verificar que enviar una `categoria_id` de otro usuario y una inexistente responde `400` con el código `REFERENCIA_INVALIDA` en ambos casos, con respuestas indistinguibles entre sí, y que no se crea ninguna tarea
- [x] 5.8 Verificar que enviar tres etiquetas de las cuales una es de otro usuario responde `400` y que la tarea no se crea ni queda ninguna asociación escrita
- [x] 5.9 Verificar que `PUT /api/tareas/:id` sustituye el contenido: editar enviando solo el título deja vacías descripción, categoría y fecha de vencimiento; omitir `etiquetas` las conserva; enviar `etiquetas: []` las quita todas
- [x] 5.10 Verificar que `PUT /api/tareas/:id` ignora `completada` y `estado` si llegan en el cuerpo y que el estado de la tarea no cambia por esa vía
- [x] 5.11 Verificar que `PATCH /api/tareas/:id/completar` con `{"completada": true}` responde `200` con la tarea completada y su instante de finalización registrado; que con `false` la deja no completada y con el instante vacío; y que repetir la misma petición dos veces deja el mismo estado, sin alternar
- [x] 5.12 Verificar que `PATCH /api/tareas/:id/completar` sin el campo, con `null`, con `"true"` y con `1` responde `400` en los cuatro casos y no cambia el estado de la tarea
- [x] 5.13 Verificar que `DELETE /api/tareas/:id` responde `204` sin cuerpo, que la tarea desaparece del listado, que su categoría y sus etiquetas siguen apareciendo en sus listados, y que repetir el borrado responde `404`
- [x] 5.14 Verificar que editar, completar y eliminar una tarea de otro usuario responde `404` en los tres casos, que la tarea ajena permanece intacta, y que la respuesta es idéntica a la de un identificador que no existe
- [x] 5.15 Verificar que el cuerpo de las respuestas sigue el contrato acordado: las operaciones sobre una tarea devuelven el objeto directamente y el listado un array desnudo; el `id` de la tarea, el de su categoría y los de sus etiquetas llegan como cadenas de texto; y una tarea sin etiquetas trae `[]` y no `null`
- [x] 5.16 Verificar que un cuerpo JSON ilegible en `POST`, `PUT` y `PATCH` responde `400` con `DATOS_INVALIDOS` y no `500`, y que un identificador mal formado en la ruta responde `400` o `404` sin filtrar detalles del almacenamiento
- [x] 5.17 Verificar contra el servidor las reglas de contenido: un título con espacios alrededor se guarda recortado, un título con salto de línea responde `400`, una descripción con varios párrafos se acepta y conserva sus saltos de línea, una descripción con otro carácter de control responde `400`, y un título con emojis se acepta y se devuelve sin alterar
- [x] 5.18 Verificar que se admite una fecha de vencimiento anterior a hoy, tanto al crear como al editar, y que la tarea la conserva
- [x] 5.19 Verificar que enviar 51 etiquetas en el cuerpo responde `400` y no crea ni modifica ninguna tarea

## 6. Listado con filtros, búsqueda y ordenación

- [x] 6.1 Preparar un conjunto de datos de prueba desechables para dos usuarios distintos, con tareas de las tres prioridades, completadas y no completadas, con y sin categoría, con y sin fecha de vencimiento, con etiquetas solapadas y con textos que permitan comprobar caja, acentos y flexión; documentar cómo se limpia al terminar
- [x] 6.2 Verificar que `GET /api/tareas` sin parámetros devuelve todas las tareas del usuario ordenadas de la más reciente a la más antigua por fecha de creación, cada una con su categoría y sus etiquetas, y que un usuario sin tareas recibe `200` con `[]`
- [x] 6.3 Verificar cada filtro por separado —`completada`, `categoria`, `prioridad`, el rango de fecha de vencimiento completo y con un solo extremo, y `etiquetas`— comprobando en cada caso que el resultado contiene exactamente las tareas esperadas y ninguna más
- [x] 6.4 Verificar que el rango de fecha de vencimiento es inclusivo en ambos extremos y que excluye las tareas sin fecha de vencimiento
- [x] 6.5 Verificar que el filtro por etiquetas exige todas las indicadas —dos nombres devuelven solo las tareas que tienen ambos— y que reconoce el nombre escrito con otras mayúsculas y minúsculas
- [x] 6.6 Verificar que `busqueda` encuentra por título y por descripción, ignorando caja y acentos, reconociendo una variante flexiva de la palabra, que una cadena con puntuación y comillas devuelve `200` y no error, y que un texto presente solo en la tarea de otro usuario no devuelve nada
- [x] 6.7 Verificar que los filtros se combinan como conjunción: `prioridad=alta&completada=false` devuelve solo las tareas que cumplen ambas condiciones, y que una combinación que no deja ninguna tarea responde `200` con `[]` y no un error
- [x] 6.8 Verificar cada valor de `ordenar` con ambas direcciones: `titulo` alfabético, `prioridad` en orden alta-media-baja y no alfabético, `fecha_vencimiento` con las tareas sin fecha al final en las dos direcciones, y `creado_en`; y que dos consultas idénticas sobre tareas con el mismo valor devuelven el mismo orden
- [x] 6.9 Verificar que la ordenación se aplica sobre el resultado ya filtrado, combinando un filtro y un orden en la misma petición
- [x] 6.10 Verificar que `ordenar` con un campo no admitido, `direccion` con un valor no admitido, `prioridad` fuera del conjunto, `completada` no booleano, una fecha mal formada y una `categoria` mal formada responden `400` señalando el parámetro, y que ninguno se ignora en silencio ni produce `500`
- [x] 6.11 Verificar que un `ordenar` que contenga SQL responde `400` en la validación sin llegar al repositorio, y comprobar por inspección que ningún fragmento de la consulta del listado se construye con una cadena procedente de la petición
- [x] 6.12 Verificar que los parámetros de consulta desconocidos se ignoran sin error y que los reconocidos siguen aplicándose
- [x] 6.13 Verificar el aislamiento del listado con los dos usuarios: cada uno recibe solo sus tareas, y filtrar por el identificador de una categoría ajena o por el nombre de una etiqueta que solo tiene el otro usuario devuelve `[]` y nunca una tarea ajena
- [x] 6.14 Verificar que `categoria=ninguna` devuelve exactamente las tareas sin categoría, que no colisiona con ningún identificador, y que combinado con otro filtro sigue comportándose como conjunción
- [x] 6.15 Verificar que `busqueda=` y `busqueda=%20` devuelven el mismo resultado que omitir el parámetro, con estado `200`, y que un texto de búsqueda de 201 caracteres responde `400`
- [x] 6.16 Verificar que `ordenar` sin `direccion` aplica la dirección por defecto de cada campo —creación y prioridad descendentes, título y vencimiento ascendentes— y que enviar `direccion` explícita invierte el resultado en los cuatro casos
- [x] 6.17 Verificar que repetir un filtro de valor único en la URL (`prioridad=alta&prioridad=media`) responde `400` y no aplica solo uno de los dos valores en silencio
- [x] 6.18 Eliminar todos los datos de prueba creados en esta fase y verificar que las tablas quedan como estaban

## 7. Cierre

- [x] 7.1 Ejecutar `openspec validate add-tareas --strict` y verificar que pasa sin avisos
- [x] 7.2 Repasar `proposal.md`, `design.md` y las specs frente a lo implementado, y registrar como decisión fechada cualquier desviación que haya aparecido durante la implementación, en particular si la configuración de texto `spanish` no estaba disponible
